// the basics - ex7.js

let foo = ['a', 'b', 'c'];
console.log(foo.length);
console.log(foo[3]);
// No, there will be no error, but an undefined is returned because JS always
// returns an undefined when accessing non-existent elements in an array.
