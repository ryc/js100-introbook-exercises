// variables - ex3.js

{
  let foo = 'bar';
}

console.log(foo); // will return a ReferenceError because the variable foo 
// has a block scope and is not available outside the block where it is being
// accessed.
